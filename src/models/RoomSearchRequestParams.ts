/** Represents room search request parameters. */
type RoomSearchRequestParams = {
  /** Gets a check-in date. */
  dateStart: Date;
  /** Gets an check-out date. */
  dateEnd: Date;
};

export default RoomSearchRequestParams;
